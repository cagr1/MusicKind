# Prototipo visual (referencia)

Generado en Google AI Studio el 2026-09-23 y aprobado por Carlos como referencia visual de `web/`.
No se compila ni se importa desde `web/`. Se porta vista por vista según `design/SPEC.md`.

Ver en local: `npm install --legacy-peer-deps && npm run dev` (puerto 3000).
